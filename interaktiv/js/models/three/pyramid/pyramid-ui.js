// ==========================================
// Pyramid UI — точка входа
// ==========================================
threeModels.three_pyramid = function(containerId) {
    new PyramidController(containerId);
};